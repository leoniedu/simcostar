% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{simcosta_fetch}
\alias{simcosta_fetch}
\title{Download and cache SIMCOSTA buoy data}
\usage{
simcosta_fetch(
  boia_id,
  start,
  end,
  endpoint = c("standard", "currents"),
  use_cache = TRUE,
  wide = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{boia_id}{Integer buoy identifier.}

\item{start}{Start of the time window. Accepts \code{POSIXct}, \code{Date},
\code{character} (parsed as UTC), or \code{numeric} Unix timestamp.}

\item{end}{End of the time window (same types as \code{start}).}

\item{endpoint}{Character vector of endpoints to fetch. One or both of
\code{"standard"} and \code{"currents"}. Defaults to both.}

\item{use_cache}{If \code{TRUE} (default), use the local SQLite cache. If \code{FALSE},
always fetch from the API and do not read from or write to the cache.}

\item{wide}{If \code{TRUE} (default), return data in wide format with friendly
column names via \code{\link[=simcosta_wide]{simcosta_wide()}}. If \code{FALSE}, return long format with
columns \code{boia_id}, \code{time}, \code{variable}, \code{value}.}

\item{verbose}{If \code{TRUE} (default), print progress messages.}
}
\value{
A \code{data.frame}. If \code{wide = TRUE}, one column per variable with
descriptive names and a \code{datetime} POSIXct column. If \code{wide = FALSE},
long format with columns \code{boia_id}, \code{time}, \code{variable}, \code{value}.
}
\description{
Downloads oceanographic and meteorological buoy data from the SIMCOSTA
platform and stores the results in a local SQLite cache.
Repeated calls only fetch time windows not yet in the cache.
}
\details{
All data is returned in long format with columns \code{boia_id}, \code{time},
\code{variable}, and \code{value}. Standard variables include wave height, water
temperature, salinity, chlorophyll, etc. Current-direction profiles are
stored as \code{direction_cell_1}, \code{direction_cell_2}, etc.
}
\section{API configuration}{


The base URL defaults to \verb{https://simcosta.furg.br/api} and can be
overridden via \code{options(rsimcosta.api_url = "...")} or the
\code{SIMCOSTA_API_URL} environment variable.

Requests are retried up to 3 times on transient failures.
}

\examples{
\dontrun{
res <- simcosta_fetch(
  boia_id = 515,
  start   = "2025-01-01",
  end     = "2025-01-02"
)
head(res)
}
}
